name: CD Omnamah.

on: push

concurrency:
  group: Deployments
  
jobs:
  build-main: 
     runs-on: ubuntu-latest
     if: ${{ github.ref == 'main' && github.event_name == 'pull_request' }}
     
     steps:
      - name: sample-run
        run: echo Hi
